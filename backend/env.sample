# Copy this file to .env in the same directory and adjust values
# 
# IMPORTANT: Set your MySQL password in the .env file
# If your MySQL doesn't require a password, leave DB_PASSWORD empty
# Example: DB_PASSWORD=your_password_here
#
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=nutrimind
DB_CONN_LIMIT=10
